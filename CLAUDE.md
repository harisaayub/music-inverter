# Code style

- Split code across multiple files by concern — not total encapsulation, but functions should be exported from their own modules rather than inlined in one file.
- Always use verbose, descriptive variable names. No abbreviations, no single-letter names, no terse shorthands.
